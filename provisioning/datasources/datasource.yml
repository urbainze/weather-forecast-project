apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: host.docker.internal:5433
    access: proxy
    isDefault: true
    database: ${DATA_DB}
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}